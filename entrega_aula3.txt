    1  sudo apt update && sudo apt upgrade -y
    2  sudo rm /var/lib/dpkg/lock-frontend
    3  sudo rm /var/lib/dpkg/lock
    4  sudo dpkg --configure -a
    5  sudo dpkg --configure -a --force-all
    6  sudo mv /etc/passwd /etc/passwd.bak && sudo cp /etc/passwd.bak /etc/passwd && sudo dpkg --configure -a
    7  sudo dpkg --configure -a
    8  sudo mkdir -p /run/systemd/resolve && sudo dpkg --configure -a
    9  sudo ln -sf /bin/true /usr/bin/systemd-sysusers && sudo dpkg --configure -a
   10  git --version
   11  git config --global user.name "Gabriel Oliveira"
   12  git config --global user.email "reformedboni@gmail.com"
   13  ssh-keygen -t ed25519 -C "reformedboni@gmail.com"
   14  cat ~/.ssh/id_ed25519.pub
   15  mkdir exercicio-remoto
   16  cd exercicio-remoto
   17  git init -b main
   18  touch README.md
   19  nano README.md
   20  git add README.md
   21  git commit -m "inicio"
   22  git remote add origin git@github.com:Refboni/exercicio-remoto.git
   23  git push -u origin main
   24  git log --oneline
   25  git log
   26  history 50 > entrega_aula3.txt
